// Lesson icon for the curriculum sidebar. Content is now editor-rendered and
// no longer typed per lesson, so a single generic icon is used throughout.
import { FileText, type LucideIcon } from "lucide-react";

export const LESSON_ICON: LucideIcon = FileText;
