{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Exports carry explicit annotations, so tsdown generates .d.ts via oxc
    // (fast path) instead of tsc.
    "isolatedDeclarations": true
  },
  "include": ["src/**/*.ts"]
}
