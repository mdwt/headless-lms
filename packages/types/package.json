{
  "name": "@headless-lms/types",
  "version": "0.0.0",
  "private": true,
  "description": "Domain types for the platform: entities, DTOs, domain events, and the integration contract. Pure type declarations — no runtime code, no dependencies.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsdown": "^0.22.9",
    "typescript": "^5.7.2"
  }
}
