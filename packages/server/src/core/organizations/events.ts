// organizations context — domain events, owned by @headless-lms/types.
export type {
  OrganizationEvent,
  InvitationCreated,
  InvitationCanceled,
  InvitationAccepted,
} from '@headless-lms/types';
