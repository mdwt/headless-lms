// entitlements context — domain events, owned by @headless-lms/types.
export type {
  EnrollmentCreated,
  EnrollmentUpdated,
  EnrollmentDeleted,
  EnrollmentExpired,
  EnrollmentEvent,
} from '@headless-lms/types';
