// entitlements context — domain events, owned by @headless-lms/types.
export type {
  EntitlementCreated,
  EntitlementUpdated,
  EntitlementDeleted,
  EntitlementExpired,
  EntitlementEvent,
} from '@headless-lms/types';
